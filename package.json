{
  "license": "MIT",
  "source": "src/index.html",
  "browserslist": ["> 0.5%, last 2 versions, not dead"],
  "scripts": {
    "clean:dist": "rimraf dist",
    "start": "yarn clean:dist && parcel",
    "build": "yarn clean:dist && parcel build"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.0",
    "parcel": "^2.0.1",
    "postcss": "^8.3.11",
    "rimraf": "^3.0.2",
    "sharp": "^0.29.1",
    "tailwindcss": "^2.2.19"
  }
}
